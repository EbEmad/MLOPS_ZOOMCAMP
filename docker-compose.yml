services:
  python_app:
    build:
      context: .
      dockerfile: dockerfile
    container_name: python_app
    hostname: python_app
    volumes:
      - ./homework:/app/homework
    ports:
      - "8000:8000"
